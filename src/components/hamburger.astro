<div class="hamburger">
    <div class="lines"></div>
    <div class="lines"></div>
    <div class="lines"></div>
</div>

<style>

    :root{
        --size: 1.2em;
        --width-mult: 1.25;
        --line-height: 0.05em;
    }

    .hamburger{
        cursor: pointer;
        height: var(--size);
        width: calc(var(--width-mult) * var(--size)) ;
        display: flex;
        flex-direction: column;
        justify-content: space-around;
        
        .lines {
            height: var(--line-height);
            min-height: var(--line-height) ;
            background-color: var(--primary-color); ;
            border: 0;
        }
    }
</style>